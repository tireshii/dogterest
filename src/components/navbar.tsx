const Navbar = () => {
    return (
        <nav className="navbar">
            <div className="navbar-logo">
                <span>Dogsterest</span>
            </div>
        </nav>
    );
};

export default Navbar;